# AI Automated Trading System

AI 기반 주식/코인 자동매매 시스템 - 강화학습(RL) + LLM Agent 하이브리드 구조

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI Trading System                             │
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                   Layer 4: Meta Controller                    │   │
│  │            (시장 레짐 감지 + 전략 선택 + 리스크 관리)           │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                              ↕                                       │
│  ┌─────────────────────┐  ┌─────────────────────┐                   │
│  │  Layer 3: RL Agent  │  │ Layer 3: LLM Agent  │                   │
│  │    (PPO/DQN)        │  │  (Market Analysis)  │                   │
│  │  - 매매 신호 생성    │  │  - 뉴스/감성 분석   │                   │
│  │  - 포지션 사이징    │  │  - 시장 컨텍스트    │                   │
│  └─────────────────────┘  └─────────────────────┘                   │
│                              ↕                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Layer 2: Feature Engineering                     │   │
│  │     기술 지표 (100+) | 온체인 데이터 | 소셜 감성 | 뉴스        │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                              ↕                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Layer 1: Trading Engine                          │   │
│  │        Binance Connector  |  Alpaca Connector  |  Backtester  │   │
│  └──────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │           Investment Journal & Analytics System               │   │
│  │  - Trade Logger | Performance Analytics | AI Feedback Loop    │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## 주요 기능

### 🤖 AI 시스템
- **강화학습 에이전트**: PPO/DQN 알고리즘 기반 매매 신호 생성
- **LLM 에이전트**: GPT 기반 시장 분석 및 뉴스 감성 분석
- **하이브리드 전략**: RL + LLM 신호 결합 (가중치 조절 가능)

### 📊 기술 지표
- 100+ 기술 지표 지원 (SMA, EMA, RSI, MACD, Bollinger Bands 등)
- 사용자 정의 신호 생성
- 피보나치, 피벗 포인트 분석

### 💹 거래소 연동
- **Binance**: 암호화폐 거래 (현물/선물)
- **Alpaca**: 미국 주식 거래

### 📈 투자 저널
- 모든 거래 자동 기록
- 성과 분석 (Sharpe, Sortino, Calmar, MDD 등)
- AI 기반 피드백 루프

### 🖥️ 대시보드
- 실시간 포트폴리오 모니터링
- 성과 차트 및 분석
- AI 인사이트 및 개선 제안

## 설치

### 요구사항
- Python 3.11+
- PostgreSQL 15+ (TimescaleDB 권장)
- Redis (선택)

### 로컬 설치

```bash
# 저장소 클론
git clone <repository-url>
cd invest

# 가상환경 생성
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 의존성 설치
pip install -e .

# 환경 변수 설정
cp .env.example .env
# .env 파일 편집하여 API 키 설정
```

### Docker 설치

```bash
# 환경 변수 설정
cp .env.example .env
# .env 파일 편집

# 기본 서비스 실행 (DB + Dashboard)
docker-compose up -d

# 페이퍼 트레이딩 포함
docker-compose --profile trading up -d

# RL 모델 학습
docker-compose --profile training up trainer
```

## 환경 변수

```bash
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/invest_db

# Binance
BINANCE_API_KEY=your_api_key
BINANCE_API_SECRET=your_api_secret
BINANCE_TESTNET=true  # 테스트넷 사용 시

# Alpaca
ALPACA_API_KEY=your_api_key
ALPACA_API_SECRET=your_api_secret
ALPACA_PAPER=true  # 페이퍼 트레이딩 모드

# OpenAI (LLM)
OPENAI_API_KEY=your_api_key
```

## 사용법

### 1. RL 모델 학습

```bash
# 기본 PPO 학습
python scripts/train_rl.py --agent ppo --symbols BTC/USDT --timesteps 100000

# DQN 학습
python scripts/train_rl.py --agent dqn --symbols BTC/USDT --timesteps 100000

# Walk-forward 최적화
python scripts/train_rl.py --agent ppo --symbols BTC/USDT --walk-forward

# 하이퍼파라미터 검색
python scripts/train_rl.py --agent ppo --symbols BTC/USDT --hyperparam-search
```

### 2. 백테스팅

```bash
# 기본 백테스트
python scripts/run_backtest.py --symbol BTC/USDT --days 180

# RL 모델 사용
python scripts/run_backtest.py --symbol BTC/USDT --rl-model models/ppo_20240101

# 주식 백테스트
python scripts/run_backtest.py --symbol AAPL --days 365
```

### 3. 페이퍼 트레이딩

```bash
# 암호화폐
python scripts/run_paper.py --symbols BTC/USDT ETH/USDT

# 주식
python scripts/run_paper.py --symbols AAPL MSFT GOOGL

# RL 모델 사용
python scripts/run_paper.py --symbols BTC/USDT --rl-model models/ppo_best
```

### 4. 실거래 (주의!)

```bash
# Dry run (기본값)
python scripts/run_live.py --symbols BTC/USDT

# 실거래 모드 (⚠️ 실제 자금 사용)
python scripts/run_live.py --symbols BTC/USDT --live
```

### 5. 대시보드

```bash
streamlit run dashboard/app.py
```

## 프로젝트 구조

```
invest/
├── config/              # 설정 관리
├── core/
│   ├── engine/          # 거래소 커넥터
│   ├── strategies/      # 트레이딩 전략
│   └── risk/            # 리스크 관리
├── ai/
│   ├── rl/              # 강화학습 (PPO, DQN)
│   ├── llm/             # LLM 에이전트
│   └── ensemble/        # 앙상블 메커니즘
├── data/
│   ├── collectors/      # 데이터 수집기
│   ├── features/        # 피처 엔지니어링
│   └── storage/         # DB 모델/저장소
├── journal/             # 투자 저널
├── dashboard/           # Streamlit 대시보드
├── backtest/            # 백테스팅 엔진
├── scripts/             # 실행 스크립트
└── tests/               # 테스트
```

## 리스크 관리

- **포지션 사이징**: 계좌의 5-10% 제한
- **일일 손실 한도**: 2% 초과 시 거래 중지
- **손절매**: ATR 기반 동적 손절
- **최대 드로다운**: 15% 초과 시 자동 중지

## 주의사항

⚠️ **투자 위험 경고**
- 이 시스템은 교육 및 연구 목적으로 제작되었습니다
- 실거래 시 자본 손실이 발생할 수 있습니다
- 충분한 백테스팅과 페이퍼 트레이딩 후 사용하세요
- 투자 결정은 본인 책임입니다

## 라이선스

MIT License

## 기여

Pull Request와 Issue를 환영합니다.
